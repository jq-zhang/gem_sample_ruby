require 'spec_helper'

describe GemSampleRuby do
  it 'has a version number' do
    expect(GemSampleRuby::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
